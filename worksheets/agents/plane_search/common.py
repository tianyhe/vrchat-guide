import os

current_dir = os.path.dirname(os.path.realpath(__file__))
prompt_dir = os.path.join(current_dir, "prompts")

botname = "Plane Search Bot"
starting_prompt = "Hello! I am a bot that helps you search for flights"
description = "You are a bot that helps prospective customers search for flights."
